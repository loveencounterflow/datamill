// Generated by CoffeeScript 2.4.1
(function() {
  'use strict';
  var $, $async, $watch, CND, H, PD, VNR, assign, badge, debug, declare, echo, first, help, info, isa, jr, last, rpr, select, size_of, stamp, type_of, types, urge, validate, warn, whisper;

  //###########################################################################################################
  H = require('./helpers');

  CND = require('cnd');

  rpr = CND.rpr;

  badge = H.badge_from_filename(__filename);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  info = CND.get_logger('info', badge);

  urge = CND.get_logger('urge', badge);

  help = CND.get_logger('help', badge);

  whisper = CND.get_logger('whisper', badge);

  echo = CND.echo.bind(CND);

  ({jr, assign} = CND);

  //...........................................................................................................
  require('./exception-handler');

  first = Symbol('first');

  last = Symbol('last');

  VNR = require('./vnr');

  //...........................................................................................................
  PD = require('pipedreams');

  ({$, $watch, $async, select, stamp} = PD);

  //...........................................................................................................
  types = require('./types');

  ({isa, validate, declare, size_of, type_of} = types);

  //-----------------------------------------------------------------------------------------------------------
  this.$trim = function(S) {
    var ref;
    ref = 'ws1/trm';
    return $((d, send) => {
      var new_text;
      if (!select(d, '^line')) {
        return send(d);
      }
      if ((new_text = d.text.trimEnd()) !== d.text) {
        d = PD.set(d, {
          text: new_text,
          ref
        });
      }
      send(d);
      return null;
    });
  };

  //-----------------------------------------------------------------------------------------------------------
  this.$blank_lines = function(S) {
    var flush, linecount, prv_dest, prv_vnr, ref, send, within_blank;
    prv_vnr = null;
    prv_dest = null;
    linecount = 0;
    send = null;
    within_blank = false;
    ref = 'ws1/bl';
    //.........................................................................................................
    H.register_key(S, '^blank', {
      is_block: false
    });
    //.........................................................................................................
    flush = (advance = false) => {
      var $vnr;
      if (prv_vnr == null) {
        return null;
      }
      within_blank = false;
      $vnr = VNR.advance(prv_vnr);
      // if advance  then  $vnr = VNR.deepen VNR.advance  prv_vnr
      // else              $vnr = VNR.deepen              prv_vnr
      ref = 'ws1/bl-A';
      send(H.fresh_datom('^blank', {
        linecount,
        $vnr,
        dest: prv_dest,
        ref
      }));
      return linecount = 0;
    };
    //.........................................................................................................
    return PD.mark_position($((pd, send_) => {
      var d, is_first, is_last, is_line;
      ({is_first, is_last, d} = pd);
      //.......................................................................................................
      send = send_;
      //.......................................................................................................
      if (is_last) {
        flush(true);
        return null;
      }
      //.......................................................................................................
      is_line = select(d, '^line');
      //.......................................................................................................
      /* Insert blank if first line isn't blank: */
      if (is_line && is_first) {
        if (d.text !== '') {
          ref = 'ws1/bl-B';
          send(H.fresh_datom('^blank', {
            linecount: 0,
            $vnr: [0],
            dest: d.dest,
            ref
          }));
        }
      }
      if (!is_line) {
        //.......................................................................................................
        return send(d);
      }
      //.......................................................................................................
      /* line is empty / blank */
      if (d.text === '') {
        if (!within_blank) {
          linecount = 0;
        }
        linecount += +1;
        within_blank = true;
        prv_dest = d.dest;
        prv_vnr = VNR.deepen(d.$vnr);
        return send(stamp(d));
      }
      if (within_blank) {
        //.......................................................................................................
        /* line contains material */
        flush(false);
      }
      prv_dest = d.dest;
      prv_vnr = d.$vnr;
      send(d);
      //.......................................................................................................
      return null;
    }));
  };

  //-----------------------------------------------------------------------------------------------------------
  this.$blank_lines_2 = function(S) {
    var ref;
    ref = 'ws1/bl2';
    H.register_key(S, '^blank', {
      is_block: false
    });
    //.........................................................................................................
    return PD.mark_position($((pd, send) => {
      var $vnr, d, is_first, is_last, sent_d;
      ({is_first, is_last, d} = pd);
      sent_d = false;
      // if S.confine_to?
      //   debug 'µ33444', ( CND.truth is_first ), ( CND.truth is_last ), jr d
      if (is_first && !select(d, '^blank')) {
        send(stamp(d, {ref}));
        $vnr = VNR.deepen(d.$vnr);
        send(H.fresh_datom('^blank', {
          $vnr: VNR.recede($vnr),
          ref
        }));
        send(PD.set(d, {
          $vnr,
          $fresh: true,
          ref
        }));
        sent_d = true;
      }
      if (is_last && !select(d, '^blank')) {
        send(stamp(d, {ref}));
        $vnr = VNR.deepen(d.$vnr);
        send(H.fresh_datom('^blank', {
          $vnr: VNR.advance($vnr),
          ref
        }));
        send(PD.set(d, {
          $vnr,
          $fresh: true,
          ref
        }));
        sent_d = true;
      }
      if (!sent_d) {
        send(d);
      }
      return null;
    }));
  };

  //-----------------------------------------------------------------------------------------------------------
  this.$blanks_at_dest_changes = function(S) {
    return $({last}, (d_, send) => {
      var db;
      if (d_ !== last) {
        return send(d_);
      }
      db = S.mirage.dbw;
      (() => {        //.........................................................................................................
        var d, ref, ref1, results, row;
        ref = 'ws1/dst1';
        ref1 = db.read_changed_dest_last_lines();
        results = [];
        for (row of ref1) {
          if (select(row, '^blank')) {
            break;
          }
          d = H.datom_from_row(S, row);
          send(stamp(d, {ref}));
          send(d = VNR.deepen(PD.set(d, {
            $fresh: true,
            ref
          })));
          results.push(send(H.fresh_datom('^blank', {
            linecount: 0,
            $vnr: VNR.advance(d.$vnr),
            dest: d.dest,
            ref
          })));
        }
        return results;
      })();
      (() => {        //.........................................................................................................
        var d, ref, ref1, results, row;
        ref = 'ws1/dst2';
        ref1 = db.read_changed_dest_first_lines();
        results = [];
        for (row of ref1) {
          if (select(row, '^blank')) {
            break;
          }
          d = H.datom_from_row(S, row);
          send(stamp(d, {ref}));
          send(d = VNR.deepen(PD.set(d, {
            $fresh: true,
            ref
          })));
          results.push(send(H.fresh_datom('^blank', {
            linecount: 0,
            $vnr: VNR.recede(d.$vnr),
            dest: d.dest,
            ref
          })));
        }
        return results;
      })();
      return null;
    });
  };

  //===========================================================================================================

  //-----------------------------------------------------------------------------------------------------------
  this.$transform = function(S) {
    var pipeline;
    pipeline = [];
    pipeline.push(this.$trim(S));
    // pipeline.push @$blank_lines             S
    pipeline.push(this.$blank_lines_2(S));
    pipeline.push(this.$blanks_at_dest_changes(S));
    return PD.pull(...pipeline);
  };

}).call(this);

//# sourceMappingURL=010-whitespace-1.js.map
