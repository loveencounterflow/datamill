// Generated by CoffeeScript 2.4.1
(function() {
  'use strict';
  var $, $async, $watch, CND, H, PD, VNR, assign, badge, debug, declare, echo, first, help, info, isa, jr, last, rpr, select, size_of, stamp, type_of, types, urge, validate, warn, whisper;

  //###########################################################################################################
  H = require('./helpers');

  CND = require('cnd');

  rpr = CND.rpr;

  badge = H.badge_from_filename(__filename);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  info = CND.get_logger('info', badge);

  urge = CND.get_logger('urge', badge);

  help = CND.get_logger('help', badge);

  whisper = CND.get_logger('whisper', badge);

  echo = CND.echo.bind(CND);

  ({jr, assign} = CND);

  //...........................................................................................................
  require('./exception-handler');

  first = Symbol('first');

  last = Symbol('last');

  VNR = require('./vnr');

  //...........................................................................................................
  PD = require('pipedreams');

  ({$, $watch, $async, select, stamp} = PD);

  //...........................................................................................................
  types = require('./types');

  ({isa, validate, declare, size_of, type_of} = types);

  //...........................................................................................................

  //-----------------------------------------------------------------------------------------------------------
  this.$paragraphs = function(S) {
    var block_depth, key_registry, prv_line_vnr, prv_was_break, within_p;
    H.register_key(S, '<p', {
      is_block: true
    });
    H.register_key(S, '>p', {
      is_block: true
    });
    key_registry = H.get_key_registry(S);
    within_p = false;
    block_depth = 0;
    prv_was_break = false;
    prv_line_vnr = null;
    //.........................................................................................................
    return $((d, send) => {
      var $vnr, dest, is_block, is_closer, is_opener, ref;
      if (PD.is_stamped(d)) {
        return send(d);
      }
      //.......................................................................................................
      is_block = key_registry[d.key].is_block;
      is_opener = select(d, '<');
      is_closer = select(d, '>');
      if (is_block) {
        if (is_opener) {
          block_depth++;
        } else {
          block_depth--;
        }
      }
      if (block_depth !== 0) {
        return send(d);
      }
      //.......................................................................................................
      if (select(d, '^break')) {
        prv_was_break = true;
        send(stamp(d));
        debug('µ440098', jr(d));
        if (within_p) {
          ref = 'µ15603';
          dest = d.dest;
          if (prv_line_vnr == null) {
            throw new Error("µ44982");
          }
          $vnr = VNR.new_level(prv_line_vnr, 0);
          prv_line_vnr = null;
          // $vnr          = VNR.new_level d.$vnr, 0
          // $vnr          = VNR.advance $vnr; send PD.set d, '$vnr', $vnr
          $vnr = VNR.advance($vnr);
          send(H.fresh_datom('>p', {$vnr, dest, ref}));
          within_p = false;
          prv_was_break = false;
        }
      }
      //.......................................................................................................
      if (select(d, '^line')) {
        prv_line_vnr = d.$vnr;
        if (prv_was_break) {
          ref = 'µ15604';
          dest = d.dest;
          $vnr = VNR.new_level(d.$vnr, 0);
          $vnr = VNR.advance($vnr);
          send(H.fresh_datom('<p', {$vnr, dest, ref}));
          $vnr = VNR.advance($vnr);
          send(PD.set(d, '$vnr', $vnr));
          prv_line_vnr = $vnr;
          within_p = true;
          prv_was_break = false;
          send(stamp(d));
        } else {
          send(d);
        }
      }
      //.......................................................................................................
      return send(d);
    });
  };

  //-----------------------------------------------------------------------------------------------------------
  this.$experiment = function(S) {
    H.register_key(S, '^x', {
      is_block: false
    });
    //.........................................................................................................
    return $({last}, (d, send) => {
      if (d !== last) {
        return send(d);
      }
      send(H.fresh_datom('^x', {
        $vnr: [10, -1],
        dest: 'xxx'
      }));
      send(H.fresh_datom('^x', {
        $vnr: [10, 0],
        dest: 'xxx'
      }));
      return null;
    });
  };

  //===========================================================================================================

  //-----------------------------------------------------------------------------------------------------------
  this.$transform = function(S) {
    var pipeline;
    pipeline = [];
    pipeline.push(this.$paragraphs(S));
    pipeline.push(this.$experiment(S));
    return PD.pull(...pipeline);
  };

}).call(this);

//# sourceMappingURL=030-2-paragraphs-consolidate.js.map
